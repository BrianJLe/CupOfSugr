<h1> Reservester </h1>  

  <div class="row">
  	<div class="large-8 columns">
      <% if !@restaurants.nil? %>
  		<% @restaurants.each do |res| %>
  		<h5>
  			<%= res.name %></h5>
  			<br>
  			<% end %>
        <% end %>
  		</div>
  	</div>

  	<%= link_to 'Add new restaurant', new_restaurant_path %>